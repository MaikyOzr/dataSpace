name: dotnet-build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore DataScribeCSP/DataScribeCSP.sln

      - name: Build
        run: dotnet build path/to/your/project.sln --no-restore

      - name: Test
        run: dotnet test path/to/your/project.sln --no-build

      - name: Publish
        run: dotnet publish path/to/your/project.sln -c Release -o /publish

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: published-app
          path: /publish
